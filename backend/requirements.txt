Flask==3.0.3
Werkzeug==3.0.1
mysql-connector-python==9.4.0
scikit-learn==1.6.1
nltk==3.9.2
python-dotenv==1.1.1

# Production WSGI server. Flask's built-in server is for development only and
# warns as much on every start; hosts run the app through this instead.
gunicorn==23.0.0

# Optional — only needed for the grounded fallback (see README). The chatbot
# runs without it: llm.py imports the SDK lazily, so a missing package simply
# means the fallback stays off.
google-genai==1.47.0
